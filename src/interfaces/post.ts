export interface PostInterface {
  metadata: {
    category: string
    cover: string
    date: string
    excerpt: string
    id: string
    link: string
    slug: string
    time: string
    title: string
  }
}
